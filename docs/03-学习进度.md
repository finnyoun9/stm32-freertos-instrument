# 03 · 学习进度（主表）

> 用法：每完成一条，把「状态」打 ✅，并在「我的实现」里写清**改了哪个文件、加了哪个函数**。
> 「证据」列指向 `docs/evidence/`、`docs/images/` 或 `firmware/` 下的具体文件。
>
> 状态约定（2026-09-18 起）：
> - ✅ 有实现 **且** 有可复现证据（日志 / 截图 / 实测数字 / 二进制验证）
> - 🟡 有实现，缺运行证据
> - ☐ 未做 ｜ ⏭ 跳过（写明原因）

> 📌 **2026-09-18 回填**：从 bench 的 `D:\Project\FreeRTOS Worksapce\demo` 把完整工程导入 `firmware/my/demo`，「我的实现」列已按**实际代码**填。
> 但**实测证据还基本是空的**，所以绝大多数是 🟡——**下一步是把 🟡 变成 ✅**。

## 统计

| 阶段 | 条目数 | ✅ | 🟡 | ☐ |
| --- | --- | --- | --- | --- |
| 第一部分 基础与移植 | 12 | 4 | 5 | 3 |
| 第二部分 任务与调度 | 8 | 0 | 8 | 0 |
| 第三部分 内存与同步 | 23 | 0 | 21 | 2 |
| 成品功能（示波器 / 万用表 / 信号发生器） | 8 | 1 | 7 | 0 |
| **合计** | **51** | **5** | **41** | **5** |

---

## 第一部分 · 基础与移植

| # | 章节 / 内容 | 我学到的 | 状态 | 我的实现（文件 / 函数） | 证据 |
| --- | --- | --- | --- | --- | --- |
| 1 | 1.1 理查德巴利与 FreeRTOS | 它是什么、为什么存在 | ☐ | — | — |
| 2 | 1.2 部署开发环境 | 工具链搭起来 | ✅ | STM32CubeIDE 工程 `demo`（`.ioc` + `.launch` + GCC），可编译出 `demo.elf` | `firmware/my/demo/demo.ioc`；`docs/02` |
| 3 | 1.3 介绍开发板的功能 | 板上资源与引脚 | ✅ | STM32F103RCT6 + 全部外设引脚表 | `docs/01`；`demo.ioc` |
| 4 | 1.4 初次使用 FreeRTOS | 第一个任务跑起来 | 🟡 | `main.c` L128-138：4 个任务 + `vTaskStartScheduler()` | 缺串口日志 |
| 5 | 2.1 FreeRTOS 总图 | 内核模块全景 | ☐ | — | — |
| 6 | 2.2 内核的文件结构 | 每个 .c 负责什么 | 🟡 | `FreeRTOS/Src`：`tasks.c` / `queue.c` / `timers.c` / `event_groups.c` / `list.c` / `heap_4.c` / `port.c` | 目录即证据 |
| 7 | 2.3 配置文件 | `FreeRTOSConfig.h` 各项含义 | ✅ | 关键宏实际值表（72MHz / 100Hz tick / 8KB 堆 / 5 档优先级 …） | `docs/02` 配置表；`Core/Inc/FreeRTOSConfig.h` |
| 8 | 2.4 安装中断服务程序 | `SVC/PendSV/SysTick` 怎么接进来 | ✅ | `startup_stm32f103rctx.s` L144/147/148 直接把向量指向 port 函数 | **`docs/evidence/20260918-vector-table/`（ELF 向量表实测）** |
| 9 | 3.1 变量的作用域和生命周期 | 作用域与生命周期 | ☐ | — | — |
| 10 | 3.2 变量在 SRAM 里的分布 | 段分布 / 栈堆生长方向 | ☐ | — | — |
| 11 | 3.3 堆 / 3.4 栈 | 堆与栈的区别、任务栈是什么 | 🟡 | `heap_4` + `configTOTAL_HEAP_SIZE=8192`；任务栈 128/256 word | 未做 map 分析 |
| 12 | 3.5 FreeRTOS 的代码规范 | 命名与类型约定 | 🟡 | 命名风格：`vXxxTask` / `xDmmData` / `prvCalcDMMValue` / `Key_HandleTypeDef` | 代码可查 |

## 第二部分 · 任务与调度

| # | 章节 / 内容 | 我学到的 | 状态 | 我的实现 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 13 | 4.1 创建任务 | `xTaskCreate` 各参数 | 🟡 | `main.c` L134-137（Key/Buzzer/LCD/LED，优先级 3/1/1/0） | 缺运行时确认 |
| 14 | 4.2 任务的 4 种状态 | 就绪/运行/阻塞/挂起 | 🟡 | `key_task.c` 用 `vTaskDelay` 阻塞；`buzzer_task` 阻塞在信号量；`lcd_task` 阻塞在事件组 | 三种阻塞源都真实存在 |
| 15 | 4.3 抢占式与协作式调度 | 两种调度策略差别 | 🟡 | `configUSE_PREEMPTION=1`；按键(3) 抢占 LCD/Buzzer(1) | `FreeRTOSConfig.h` |
| 16 | 补充 · 为什么要用 `vTaskDelay()` | 让出 CPU 的正确姿势 | 🟡 | `key_task.c`：扫描后 `vTaskDelay(pdMS_TO_TICKS(10))` | 代码可查 |
| 17 | 4.4 时间片 | 同优先级轮转 | 🟡 | `configUSE_TIME_SLICING=1`；Buzzer 与 LCD 同为优先级 1 | 缺观察证据 |
| 18 | p1.1 为 KEY1 编写驱动 | 独立按键驱动 | 🟡 | `App/Tasks/Keys/key1.c` + `App/Drivers/key.c` | 代码可查 |
| 19 | p1.2 按钮驱动的复用 | 抽象与复用 | 🟡 | 一份 `Key_Scan()` 复用出 6 个实例（KEY1-4 / UP / DOWN），回调注册式 | 代码可查 |
| 20 | p1.3 拆分超长代码 | 模块化 | 🟡 | 分层 `App/{Drivers, GUI, Tasks}`，GUI 每个面板一个文件 | 目录即证据 |

## 第三部分 · 内存与同步

| # | 章节 / 内容 | 我学到的 | 状态 | 我的实现 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 21 | 5.1 为什么需要堆内存管理 | 裸 malloc 的问题 | 🟡 | 工程用 `heap_4.c`，任务栈/队列/TCB 全部从 8KB 托管堆分配 | `FreeRTOS/Src/heap_4.c` |
| 22 | 5.2 五种堆内存管理方案 | heap_1~5 差异 | 🟡 | 只编译 `heap_4.c`（可 free + 合并相邻空闲块） | 工程文件列表 |
| 23 | 5.3 内存使用分析 | 看 map / 统计占用 | ☐ | — | 待做 |
| 24 | 5.4 选择哪种堆方案 | 选型依据 | 🟡 | 选 heap_4：有删除/释放需求且要抗碎片 | `docs/05` 设计问题 |
| 25 | 5.5 堆内存优化 | 优化动作与前后数字 | ☐ | — | 待做 |
| 26 | 6.1 二进制信号量 | 同步/互斥的用法 | 🟡 | `lcd_task.c`：`xSemaphoreForDMA` 等 SPI DMA 发完 | 代码可查 |
| 27 | p2.1 按钮音效 | 按键 → 蜂鸣器 | 🟡 | `App/Tasks/buzzer_task.c`：`Buzzer_Beep()` 给信号量 | ⚠️ 蜂鸣器输出代码被注释 |
| 28 | 6.2 在中断里使用二进制信号量 | `FromISR` + 中断到任务解耦 | 🟡 | `HAL_SPI_TxCpltCallback` → `xSemaphoreGiveFromISR` + `portYIELD_FROM_ISR` | 代码可查 |
| 29 | p3.1 在 LCD 上显示图像 | LCD 显示原理 | 🟡 | `App/Tasks/lcd_task.c` + 预编译库 `lcd.a` | 代码可查 |
| 30 | p3.2 发送数据和命令 | 命令/数据通道 | 🟡 | `SendCommand()` / `SendData()`，用 RS 引脚区分，NSS 片选 | 代码可查 |
| 31 | p3.3 显示图像 | 打点/刷屏 | 🟡 | `App/GUI/*` 每个面板自绘（`LCD_FillRect` / `LCD_PrintString` / `LCD_DrawWaveform`） | 代码可查 |
| 32 | p3.4 二进制信号量 + DMA | DMA 完成唤醒任务 | 🟡 | `HAL_SPI_Transmit_DMA` + `xSemaphoreTake(portMAX_DELAY)` | 代码可查 |
| 33 | p4.1 初次使用 LCD 驱动 | 用现成驱动 | 🟡 | `LCD_Init`（回调式：reset / send_command / send_data） | 代码可查 |
| 34 | 7.1 计数信号量的基本用法 | 计数语义 | 🟡 | `xSemaphoreCreateCounting(5, 0)` 用于蜂鸣器请求 | `buzzer_task.c` |
| 35 | 7.2 计数信号量的两种典型用法 | 事件计数 / 资源管理 | 🟡 | 用「事件计数」语义：连按积累请求，最多积压 5 次 | 代码可查 |
| 36 | 8.1 队列的基本用法 | 队列读写与阻塞 | 🟡 | `dmm.c`：`xQueueCreate(1, sizeof(DMMData_t))` 邮箱 | 代码可查 |
| 37 | 8.2 守门员任务 | 单一资源所有权 | 🟡 | LCD 任务独占 SPI + 屏幕，其它模块只发「重绘位」，不直接写屏 | `lcd_task.c` |
| 38 | 8.3 按值拷贝和按引用拷贝 | 拷贝语义与坑 | 🟡 | `xQueueOverwriteFromISR(&xDmmData)` 传结构体（按值拷贝） | 代码可查 |
| 39 | 8.4 邮箱 | 队列长度为 1 的用法 | 🟡 | `xQueueOverwriteFromISR`（ISR 写）+ `xQueuePeek`（任务读，不取走） | 代码可查 |
| 40 | p6.1 邮箱和数字万用表 | 采集 → 队列 → 计算 | 🟡 | `HAL_ADCEx_InjectedConvCpltCallback` → 算完直接覆盖邮箱 | 代码可查 |
| 41 | p6.2 计算数字万用表的结果 | 采样值与物理量换算 | 🟡 | `prvCalcDMMValue()`：电压挡 ×1/×5/×25，电阻挡 `Rx = Vin/(2.5−Vin)×R0` | `dmm.c` L135-181 |
| 42 | p6.3 使用内部参考源改善测量结果 | `VREFINT` 校准 | 🟡 | `VDDA = 4095/VREFINT_raw×1.205`，每次都实时反推 | `dmm.c` L58 |
| 43 | 9.1 软件定时器的基本用法 | 定时器任务与回调 | 🟡 | `waveform_capture.c`：Hold-Off（10ms 单次）+ Force Trigger（100ms 周期） | 代码可查 |

## 第四部分 · 成品（示波器 + 万用表 + 信号发生器）

| # | 内容 | 状态 | 我的实现 | 证据 |
| --- | --- | --- | --- | --- |
| 44 | 免费部分之外的章节（事件组 / 互斥量 / 任务通知 / 流缓冲） | 🟡 | **事件组**已实战（`lcd_task.c` 9 个重绘位）；互斥量与流缓冲未用；任务通知已开启未用 | `FreeRTOSConfig.h` + `lcd_task.c` |
| 45 | 成品：整机功能与任务架构 | ✅ | 三合一仪器 + 4 任务 + 三层分层 | `docs/05`；`main.c` |
| 46 | 示波器：采样链路（ADC + DMA + 缓冲） | 🟡 | `waveform_capture.c`：TIM3 触发 → ADC1 常规序列 → DMA 1024 点 → 换算 | 计算值 128.1kSa/s，**待上板实测** |
| 47 | 示波器：显示与交互（时基 / 幅度 / 触发） | 🟡 | `scale_panel.c`（12 档时基 / 5 档幅度）、`run_mode_panel.c`（连续/单次）、EXTI3 触发 + Hold-Off | 待上板确认 |
| 48 | 示波器：测量 | 🟡 | 实现的是**光标法**（X1/X2/Y1/Y2 + ΔX/ΔY），**没有**自动 Vpp/频率计算 | 如实记录，别在简历写「自动测量」 |
| 49 | 数字万用表：采样与量程换算 | 🟡 | `dmm.c`：注入序列 10Hz + 6 挡位识别 + 换算公式 | 待与台式表对比 |
| 50 | 数字万用表：内部参考源校准 | 🟡 | VREFINT 反推 VDDA（已有代码，未记录校准前后误差） | 待补对比数据 |
| 51 | 信号发生器（DAC + DMA + TIM5） | 🟡 | `waveform_generator.c`：正弦/三角/方波/OFF，≤500kHz 更新率 | ⚠️ 三角波只有上升段，待确认 |

> 第四部分的分节以课程资料为准，看到哪补到哪。

---

## 进度流水（按日期）

| 日期 | 做了什么 | 卡在哪 / 结论 |
| --- | --- | --- |
| 2026-04~05 | 在 bench 上跟课完成 demo 工程（按键→音效→LCD→信号量→队列/邮箱→万用表→软定时器→示波器→信号发生器） | 代码完成，构建产物 `demo.elf`（2026-08-03） |
| 2026-09-18 | 从 bench 导入完整工程到 `firmware/my/demo`，并核对引脚、配置、任务与机制实现 | 实测证据缺失，51 条里 41 条是 🟡 |
