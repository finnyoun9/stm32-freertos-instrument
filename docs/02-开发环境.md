# 02 · 开发环境与 FreeRTOS 移植

> 记录「从装工具到内核跑起来」这一段的每一步，尤其是**移植时改了哪些文件、为什么**。

## 工具链

| 工具 | 版本 | 用途 |
| --- | --- | --- |
| Keil MDK | 待填 | 编译 / 下载 / 调试 |
| STM32CubeMX | 待填 | 时钟树、外设初始化代码生成 |
| STM32CubeProgrammer / ST-Link Utility | 待填 | 烧录 |
| ST-Link 驱动 | 待填 | SWD 调试 |
| 串口工具 | 待填（如 XCOM / MobaXterm） | 日志 |
| 逻辑分析仪 / 示波器 | 待填 | 时序与波形验证 |
| FreeRTOS 源码版本 | 待填（如 V10.4.6 / V11.x） | 内核 |

## 工程结构

```text
待填：把实际工程的目录树贴进来（Core/ Drivers/ Middlewares/FreeRTOS/ …）
```

| 目录 | 内容 |
| --- | --- |
| 待填 | |

## FreeRTOS 移植步骤（按实际改动记录）

| 步骤 | 具体动作 | 涉及文件 | 结果 |
| --- | --- | --- | --- |
| 1. 取源码 | 待填（从课程资料 / 官网 / GitHub 取哪个版本） | — | |
| 2. 放入工程 | 待填（放到哪个目录、加进 Keil 分组） | | |
| 3. 选 port | 待填（`portable/RVDS/ARM_CM4F` 或 `GCC/ARM_CM4F`，为什么选它） | `port.c` | |
| 4. 配置头文件 | 待填（`configCPU_CLOCK_HZ`、`configTICK_RATE_HZ`、`configTOTAL_HEAP_SIZE`、`configMAX_PRIORITIES`…） | `FreeRTOSConfig.h` | |
| 5. 安装中断服务程序 | 待填（`SVC_Handler` / `PendSV_Handler` / `SysTick_Handler` 如何映射到 FreeRTOS 的 `xPortPendSVHandler` 等） | `FreeRTOSConfig.h` / `stm32fxxx_it.c` | |
| 6. 优先级分组 | 待填（`HAL_NVIC_SetPriorityGrouping` 取哪一组，与 `configPRIO_BITS` 是否一致） | `main.c` | |
| 7. 首次运行 | 待填（第一个任务闪灯/串口打印，怎么确认调度器真的跑起来了） | | |

## 关键配置项（照抄实际值，别抄教程默认值）

| 宏 | 我设的值 | 为什么 |
| --- | --- | --- |
| `configCPU_CLOCK_HZ` | 待填 | |
| `configTICK_RATE_HZ` | 待填 | |
| `configMAX_PRIORITIES` | 待填 | |
| `configMINIMAL_STACK_SIZE` | 待填 | |
| `configTOTAL_HEAP_SIZE` | 待填 | |
| `configUSE_PREEMPTION` | 待填 | |
| `configUSE_TIME_SLICING` | 待填 | |
| `configUSE_IDLE_HOOK` / `configCHECK_FOR_STACK_OVERFLOW` | 待填 | |
| `configLIBRARY_MAX_SYSCALL_INTERRUPT_PRIORITY` | 待填 | |

## 移植踩的坑

| 现象 | 原因 | 怎么改的 |
| --- | --- | --- |
| 待填（例如：一进调度器就 HardFault） | | |
| 待填（例如：中断里调用非 FromISR 版本） | | |

## 验证方法（怎么证明移植成功）

- [ ] 两个不同优先级任务交替打印，串口能看到固定顺序 → 抢占生效
- [ ] 同优先级任务加时间片 → 交替输出
- [ ] `xPortGetFreeHeapSize()` 打印，确认堆在按预期消耗
- [ ] `vTaskList()` / `uxTaskGetStackHighWaterMark()` 输出任务状态与栈余量
- [ ] 待填

## 证据

| 文件 | 说明 |
| --- | --- |
| `docs/evidence/` | 编译输出、串口日志、`vTaskList` 截图 |
