# 08 · 待补与下一步

> 这里放**没做完、没验证、说不清**的东西。写进来不丢人，假装做完才丢人。
> 2026-09-18 更新：完整工程已从 bench 导入，**瓶颈从「没有代码」变成「没有实测证据」**。

## ⛔ 阻塞项（先解决这个）

| 项 | 为什么阻塞 | 怎么解 |
| --- | --- | --- |
| **源码归属未确认** | 工程文件头作者都是 `liuyu`（课程模板），无法区分「我写的」和「照抄的」。**不确定就不能写简历，也不能在面试里说「我实现的」** | 见 `firmware/README.md` 的「待确认：源码归属」，逐模块标注 |

## 待验证（有实现但没证据 → 51 条里有 41 条是 🟡）

| 项 | 现在能说到什么程度 | 补什么证据 |
| --- | --- | --- |
| 采样率 128.1 kSa/s | 只有计算值（TIM3 PSC=0/ARR=561） | 定时器翻转 IO + 示波器量周期 |
| 波形刷新率 | 未知 | 逻辑分析仪测 SPI 帧间隔（或屏幕录像计数） |
| 万用表精度 | 只有换算公式 | 与台式万用表多点对比，记录误差 |
| 信号发生器频率/幅值误差 | 只有代码 | 用本机示波器量输出（bench 上有 DS1202Z-E，可用脚本抓） |
| 时基准确性 | 未知 | 输入已知频率方波，比对标称值 |
| VREFINT 校准效果 | 有代码，无数据 | 记录校准前后同一电压的读数差异 |
| 任务栈余量 / 堆余量 | **查不了**（`INCLUDE_uxTaskGetStackHighWaterMark=0`、`configUSE_TRACE_FACILITY=0`） | 先开宏，再打印 |
| 中断 → 任务链路的时序 | 有代码 | 逻辑分析仪抓 EXTI3 / ADC DMA / SPI NSS 的时序图 |

## 待补（没做 / 没写）

| 项 | 为什么重要 | 计划 |
| --- | --- | --- |
| 运行时观测（栈水位/堆/CPU） | 第 9 章的东西一个都没验证，面试深挖调度必问 | 打开 4 个宏，跑一次并记录 |
| `configCHECK_FOR_STACK_OVERFLOW` | 现在栈溢出查不出来，属隐患 | 打开后做一次故意溢出的实验 |
| DMA2_Channel3（DAC）优先级 0 的隐患 | 优先级高于 `configMAX_SYSCALL_INTERRUPT_PRIORITY`，一旦在回调里调用 API 就会炸 | 降到 ≥5，或确认回调干净 |
| 三角波只有上升段 | 可能是 bug（看起来会像锯齿） | 上板确认，是 bug 就修 |
| 蜂鸣器输出被注释 | 功能清单里写了「音效」但实际没响 | 恢复并实测 |
| 第五章之后的教材章节 | 事件组已实战，互斥量/流缓冲未用 | 按需补 |
| 实物照片 / 上电电流 | `docs/01` 的实测表全空 | 拍照片 + 用 DP100 量 |
| 课程配套源码归档 | 「课程 vs 我的」要能对照 | 从课程资料取对应章节 example（目前只到 p1.2） |

## 待写清（数字缺测量条件）

| 结论 | 缺什么 |
| --- | --- |
| 128.1 kSa/s | 采样时间/转换时间对有效采样率的影响（当前按 TRGO 频率算，未扣除转换开销） |
| 万用表 6 个挡位 | 分压电阻实际取值（从原理图核对，现在是从公式反推） |

## 下一步动作（按优先级）

1. [ ] **确认源码归属**，填 `firmware/README.md` 的归属表 ← 阻塞项
2. [ ] 打开观测宏（`configCHECK_FOR_STACK_OVERFLOW` / `INCLUDE_uxTaskGetStackHighWaterMark` / `configGENERATE_RUN_TIME_STATS` / `configUSE_TRACE_FACILITY`），跑一次记录栈水位/堆余量
3. [ ] 上板实测：采样率、波形刷新率、信号发生器输出（bench 上的 DS1202Z-E 可以直接抓）
4. [ ] 万用表与台式表对比，记录误差
5. [ ] 给 `docs/04` 每个机制补「我的理解」（自己的话）
6. [ ] 拍实物照片，补齐 `docs/01` 实测表
7. [ ] 把 `docs/03` 的 🟡 逐条升级成 ✅
